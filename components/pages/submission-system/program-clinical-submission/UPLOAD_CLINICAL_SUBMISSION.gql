#import "./CLINICAL_SUBMISSION_FRAGMENT.gql"

mutation($programShortName: String!, $files: [Upload!]) {
  uploadClinicalSubmissions(shortName: $programShortName, clinicalFiles: $files) {
    ...ClinicalSubmissionFragment
  }
}
