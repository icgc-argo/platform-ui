query DATA_TYPES_CHART($filters: JSON) {
  file {
    aggregations(filters: $filters, include_missing: true) {
      data_type {
        buckets {
          doc_count
          key
        }
      }
    }
  }
}
