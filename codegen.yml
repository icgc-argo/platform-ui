schema:
  # remote
  # - ${GATEWAY_API_ROOT}/graphql
  # local
  - generated/gql_schema.json
documents: [./**/*.gql, ./**/*_QUERY.ts, ./**/*_MUTATION.ts, ./**/*_FRAGMENT.ts]
generates:
  generated/gql_schema.json:
    plugins:
      - introspection
  generated/gql_types.tsx:
    plugins:
      - typescript
