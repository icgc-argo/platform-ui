import { createPage } from 'global/utils/pages';
import JoinProgramLoginPage from 'components/pages/submission-system/join/login';

export default createPage({
  isPublic: true,
})(JoinProgramLoginPage);
